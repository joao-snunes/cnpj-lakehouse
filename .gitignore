# Secrets — never commit
.env

# main.py has hardcoded AWS credentials and must be refactored to read from .env
# before being tracked again. Remove this line after the refactor.
main.py

# Local Python environment
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# IDE / OS
.idea/
.vscode/
Thumbs.db
.DS_Store

# Downloaded data and local parquet output (large)
ingestion/data/
data/
postgres_data/
*.zip

# Gold schema is still evolving — freeze it out of tracking for now so it
# doesn't churn every commit. Untracked via `git rm --cached`, not deleted.
docs/schema_gold.md